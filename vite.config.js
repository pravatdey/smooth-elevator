// Minimal CommonJS Vite config to avoid ESM plugin import issues in some environments.
// Vite will also pick up `vite.config.mjs` (ESM) when available; this file is kept
// to ensure tooling that expects a CommonJS config won't fail.
module.exports = {};
